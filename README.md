# ♻️ โครงการ Waste-Cycle (Full-stack Monorepo)

ยินดีต้อนรับสู่ Waste-Cycle! นี่คือแพลตฟอร์ม Full-stack ที่สร้างขึ้นเพื่อเชื่อมต่อเกษตรกร โดยมีเป้าหมายในการสร้างเศรษฐกิจหมุนเวียน (Circular Economy) ด้านการเกษตร

โปรเจกต์นี้ช่วยให้ **ฟาร์มปศุสัตว์** (เช่น ฟาร์มไก่, วัว, หมู) สามารถลงประกาศขาย "มูลสัตว์" และ **ฟาร์มพืช** สามารถเข้ามาค้นหาและซื้อ "ปุ๋ย" คุณภาพสูงที่ต้องการได้

## ✨ คุณสมบัติหลัก (Key Features)

* **ตลาดกลาง (Marketplace):** ค้นหา, กรอง, และดูประกาศซื้อมูลสัตว์/ปุ๋ย
* **ระบบยืนยันตัวตน (Authentication):** ล็อกอิน, สมัครสมาชิก, และจัดการ Role (Admin, Seller, User)
* **ระบบจอง (Booking System):** ผู้ซื้อสามารถจองสินค้า และผู้ขายสามารถ "ยืนยัน" หรือ "ยกเลิก" ออเดอร์ได้
* **ระบบแชท (Real-time Chat):** ผู้ซื้อและผู้ขายสามารถพูดคุยกันได้แบบ Real-time (สร้างห้องแชทสำหรับแต่ละโพสต์)
* **ระบบแจ้งเตือน (Notification System):**
    * แจ้งเตือนเมื่อมี **แชทใหม่** (ผ่าน Cloud Functions)
    * แจ้งเตือนเมื่อมี **การจองใหม่** หรือ **สถานะการจองอัปเดต**
    * แจ้งเตือนเมื่อ **Admin อนุมัติฟาร์ม**
* **ระบบจับคู่ (Matching System):** ผู้ซื้อสามารถสร้าง "คำขอ" และผู้ขายสามารถ "ยื่นข้อเสนอ" ได้
* **เครื่องมือเกษตรกร:**
    * **วิเคราะห์ NPK:** คำนวณค่า NPK จากประเภทสัตว์และปริมาณ (API-18)
    * **คำนวณปุ๋ย:** แนะนำสูตรปุ๋ยที่เหมาะกับพืช
* **แดชบอร์ด (Dashboard):** สรุปข้อมูลสถิติของฟาร์ม
* **ส่วนผู้ดูแลระบบ (Admin Panel):** ดูรายชื่อผู้ใช้, ยืนยันฟาร์ม (Approve), และจัดการระบบ (API 22-25)

---

## 🏗️ สถาปัตยกรรม (Project Structure)

โปรเจกต์นี้เป็นแบบ **Monorepo** โดยแบ่งเป็น 3 ส่วนหลัก:

1.  **`client/` (Frontend):**
    * สร้างด้วย **React + Vite**
    * ใช้ Firebase Client SDK (v9+) สำหรับ Auth และ Firestore (Real-time Chat & Notifications)
    * ใช้ Google Maps API (`@vis.gl/react-google-maps`) สำหรับแผนที่

2.  **`server/` (Backend):**
    * สร้างด้วย **Node.js + Express**
    * เป็น API หลักสำหรับจัดการ Logic ส่วนใหญ่ (Booking, Matching, Admin)
    * ใช้ **Firebase Admin SDK** สำหรับยืนยัน Token, กำหนด Role, และยิงแจ้งเตือน (Booking, Admin)

3.  **`firebase-functions/` (Serverless):**
    * ใช้ **Cloud Functions for Firebase**
    * ทำหน้าที่เดียว: ดักจับเหตุการณ์การสร้างข้อความใหม่ในแชท (`onCreate`) และยิงแจ้งเตือน (Notification) ไปหาผู้รับ

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Development)

คุณต้องรัน 3 ส่วนพร้อมกัน (เปิด 3 Terminal) เพื่อให้ระบบทำงานสมบูรณ์

### 1. (ครั้งแรก) ติดตั้ง Firebase Tools

ถ้าคุณยังไม่มี `firebase-tools` ให้ติดตั้งก่อน (จำเป็นสำหรับ `firebase-functions`)
```bash
npm install -g firebase-tools
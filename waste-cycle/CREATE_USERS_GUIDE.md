# คู่มือการสร้าง User ใน Firebase Authentication

## 🔍 ปัญหาที่พบ

เมื่อพยายาม login ด้วย email `A@gmail.com` หรือ `B@gmail.com` ได้ error:
```
auth/invalid-credential
```

**สาเหตุ:** Email เหล่านี้ยังไม่ได้ถูกสร้างใน Firebase Authentication

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ใช้ Script (แนะนำ)

1. **เปิดไฟล์ `server/scripts/create-users.js`**
   ```bash
   cd server
   code scripts/create-users.js
   ```

2. **แก้ไข email และ password ที่ต้องการสร้าง:**
   ```javascript
   const usersToCreate = [
     {
       email: 'A@gmail.com',
       password: 'password123', // ⚠️ เปลี่ยนเป็น password ที่ต้องการ
       displayName: 'User A',
     },
     {
       email: 'B@gmail.com',
       password: 'password123', // ⚠️ เปลี่ยนเป็น password ที่ต้องการ
       displayName: 'User B',
     },
   ];
   ```

3. **รัน script:**
   ```bash
   node scripts/create-users.js
   ```

4. **ตรวจสอบผลลัพธ์:**
   - ควรเห็น `✅ User created successfully` สำหรับแต่ละ user
   - ควรเห็นรายการ users ทั้งหมด

---

### วิธีที่ 2: ใช้ Firebase Console (Web UI)

1. **เปิด Firebase Console:**
   - ไปที่: https://console.firebase.google.com/
   - เลือกโปรเจกต์: `waste-cycle-a6c6e`

2. **ไปที่ Authentication:**
   - คลิก "Authentication" ในเมนูด้านซ้าย
   - คลิก "Users" tab

3. **เพิ่ม User:**
   - คลิกปุ่ม "Add user"
   - ใส่ Email: `A@gmail.com`
   - ใส่ Password: `password123` (หรือ password ที่ต้องการ)
   - คลิก "Add user"

4. **ทำซ้ำสำหรับ User B:**
   - คลิก "Add user" อีกครั้ง
   - ใส่ Email: `B@gmail.com`
   - ใส่ Password: `password123`
   - คลิก "Add user"

---

### วิธีที่ 3: ใช้ Register Page ใน App

1. **เปิดหน้า Register:**
   - ไปที่หน้า Landing Page
   - คลิก "Get Started"
   - คลิก "ลงทะเบียน" หรือ "Register"

2. **กรอกข้อมูล:**
   - Email: `A@gmail.com`
   - Password: `password123`
   - Confirm Password: `password123`
   - Name: `User A`
   - Farm Name: (ไม่บังคับ)

3. **คลิก "ลงทะเบียน"**

4. **ทำซ้ำสำหรับ User B**

---

## 🔐 ตรวจสอบ User ที่มีอยู่

### ใช้ Script:
```bash
node scripts/create-users.js
```
(script จะแสดงรายการ users ทั้งหมด)

### ใช้ Firebase Console:
1. ไปที่ Firebase Console → Authentication → Users
2. จะเห็นรายการ users ทั้งหมด

---

## ⚠️ ข้อควรระวัง

1. **Email Format:**
   - ตรวจสอบว่า email ถูกต้อง (เช่น `A@gmail.com` ไม่ใช่ `A@gmail.con`)
   - Email ต้องเป็นรูปแบบที่ถูกต้อง

2. **Password Requirements:**
   - ต้องมีความยาวอย่างน้อย 6 ตัวอักษร
   - แนะนำให้ใช้ password ที่แข็งแรง

3. **Email Verification:**
   - User ที่สร้างใหม่จะยังไม่ verified
   - สามารถ login ได้ทันที (ไม่ต้อง verify email)

4. **User Profile:**
   - หลังจาก login ครั้งแรก → user profile จะถูกสร้างอัตโนมัติใน Firestore
   - Profile จะถูกสร้างที่ collection `users` ด้วย document ID = Firebase UID

---

## 🧪 ทดสอบ Login

หลังจากสร้าง user แล้ว:

1. **เปิดหน้า Login:**
   - ไปที่หน้า Landing Page
   - คลิก "Get Started"
   - หรือไปที่หน้า Login โดยตรง

2. **Login:**
   - Email: `A@gmail.com`
   - Password: `password123` (หรือ password ที่ตั้งไว้)

3. **ตรวจสอบ:**
   - ควร login สำเร็จ
   - ควร redirect ไปหน้า Profile
   - ควรเห็นข้อมูล user ในหน้า Profile

---

## 📝 ตัวอย่างการใช้งาน Script

```bash
# 1. ไปที่โฟลเดอร์ server
cd server

# 2. แก้ไขไฟล์ scripts/create-users.js
#    - เปลี่ยน email และ password ตามต้องการ

# 3. รัน script
node scripts/create-users.js

# 4. ตรวจสอบผลลัพธ์
#    - ควรเห็น "✅ User created successfully"
#    - ควรเห็นรายการ users ทั้งหมด
```

---

## 🔧 แก้ไข Password

### ใช้ Script:
แก้ไขไฟล์ `server/scripts/create-users.js` และใช้ฟังก์ชัน `updateUserPassword`:

```javascript
await updateUserPassword('A@gmail.com', 'newPassword123');
```

### ใช้ Firebase Console:
1. ไปที่ Authentication → Users
2. คลิกที่ user ที่ต้องการ
3. คลิก "Reset password"
4. ใส่ password ใหม่

---

## ❌ ลบ User

### ใช้ Script:
แก้ไขไฟล์ `server/scripts/create-users.js` และใช้ฟังก์ชัน `deleteUser`:

```javascript
await deleteUser('A@gmail.com');
```

### ใช้ Firebase Console:
1. ไปที่ Authentication → Users
2. คลิกที่ user ที่ต้องการ
3. คลิก "Delete user"

---

## 📞 สรุป

**ปัญหาหลัก:** Email `A@gmail.com` และ `B@gmail.com` ยังไม่ได้ถูกสร้างใน Firebase Authentication

**วิธีแก้:**
1. ใช้ script `server/scripts/create-users.js` (แนะนำ)
2. ใช้ Firebase Console
3. ใช้ Register Page ใน App

**หลังจากสร้าง user แล้ว:**
- User สามารถ login ได้ทันที
- User profile จะถูกสร้างอัตโนมัติใน Firestore หลังจาก login ครั้งแรก

